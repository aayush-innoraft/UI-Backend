<?php
/**
 * Implements hook_mail().
 */
function shopping_cart_mail($key, &$message, $params) {
  switch ($key) {
    case 'order_confirmation':
      $message['subject'] = $params['subject'];
      $message['body'][] = $params['message'];
      break;
  }
}
